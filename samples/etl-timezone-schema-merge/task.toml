schema_version = "1.2"

[task]
name = "sanjith/etl-timezone-schema-merge"
description = "Merge three quarterly transaction extracts with schema drift, mixed timezones, a DST gap, and overlap."
authors = [{ name = "Sanjith" }]
keywords = ["data-science", "etl", "data-cleaning"]

[metadata]

[agent]
timeout_sec = 900.0

[verifier]
timeout_sec = 180.0

[verifier.env]

[environment]
build_timeout_sec = 900.0
os = "linux"
cpus = 2
memory_mb = 4096
storage_mb = 4096
gpus = 0
allow_internet = true
mcp_servers = []

[environment.env]

[solution.env]
