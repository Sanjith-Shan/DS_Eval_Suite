schema_version = "1.1"

[task]
name = "etl-timezone-schema-merge"
description = "Merge three quarterly transaction extracts with schema drift, mixed timezones, a DST gap, and overlap."
authors = ["Sanjith"]
keywords = ["data-science", "etl", "data-cleaning"]

[agent]
timeout_sec = 900

[verifier]
timeout_sec = 180

[environment]
allow_internet = false
cpus = 2
memory_mb = 4096
storage_mb = 2048
