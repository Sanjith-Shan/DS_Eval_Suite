schema_version = "1.2"

[task]
name = "sanjith/p3-class-imbalance-not-mentioned"
description = "Build a classifier; target is 97/3 imbalanced — naive training predicts all-majority and gets near-zero F1 on the holdout."
authors = [{ name = "Sanjith" }]
keywords = ["data-science", "implicit-constraints", "data-awareness", "class-imbalance"]

[metadata]

[agent]
timeout_sec = 900.0

[verifier]
timeout_sec = 300.0

[verifier.env]

[environment]
build_timeout_sec = 900.0
os = "linux"
cpus = 2
memory_mb = 4096
storage_mb = 4096
gpus = 0
allow_internet = true
mcp_servers = []

[environment.env]

[solution.env]
