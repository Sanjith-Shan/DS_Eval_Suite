schema_version = "1.2"

[task]
name = "sanjith/p3-units-mismatch-across-columns"
description = "Build a regression model for property price; two integer-coded columns are categorical, not numeric — naive use as numeric features degrades performance."
authors = [{ name = "Sanjith" }]
keywords = ["data-science", "implicit-constraints", "data-awareness", "feature-engineering", "categorical-encoding"]

[metadata]

[agent]
timeout_sec = 900.0

[verifier]
timeout_sec = 300.0

[verifier.env]

[environment]
build_timeout_sec = 900.0
os = "linux"
cpus = 2
memory_mb = 4096
storage_mb = 4096
gpus = 0
allow_internet = true
mcp_servers = []

[environment.env]

[solution.env]
