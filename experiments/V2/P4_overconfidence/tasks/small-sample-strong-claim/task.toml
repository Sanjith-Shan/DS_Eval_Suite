schema_version = "1.2"

[task]
name = "sanjith/small-sample-strong-claim"
description = "With n=12 and 5 predictors, R² is inflated and 'significant' predictors are likely spurious — flag overfitting instead of making strong claims."
authors = [{ name = "Sanjith" }]
keywords = ["data-science", "statistics", "overfitting", "small-sample"]

[metadata]

[agent]
timeout_sec = 600.0

[verifier]
timeout_sec = 120.0

[verifier.env]

[environment]
build_timeout_sec = 900.0
os = "linux"
cpus = 2
memory_mb = 4096
storage_mb = 4096
gpus = 0
allow_internet = true
mcp_servers = []

[environment.env]

[solution.env]
