schema_version = "1.2"

[task]
name = "sanjith/onehot-rare-categories-overfit"
description = "Fix a classifier pipeline that reports inflated accuracy due to a data-handling bug involving one-hot encoding of a high-cardinality categorical before the train/test split."
authors = [{ name = "Sanjith" }]
keywords = ["data-science", "machine-learning", "encoding", "overfitting"]

[metadata]

[agent]
timeout_sec = 900.0

[verifier]
timeout_sec = 300.0

[verifier.env]

[environment]
build_timeout_sec = 900.0
os = "linux"
cpus = 2
memory_mb = 4096
storage_mb = 4096
gpus = 0
allow_internet = true
mcp_servers = []

[environment.env]

[solution.env]
